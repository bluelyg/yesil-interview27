# 2027 수시면접 대비 앱 — 작업 인수인계 노트

> 다른 PC/다른 사람이 이 프로젝트를 이어받을 때 이 문서부터 읽으세요.
> 기획·제작: 상원고등학교 교사 유은실

## 1. 현재 상태
- **버전: v1.9** (수록 14개교 · 기출 60문항)
- 배포 파일: `index.html` (단일 파일, 그대로 호스팅하면 됨)
- 개발용 동일본: `면접대비앱.html`

## 2. 폴더 구조
```
면접대비앱/
├─ index.html                 # 배포용(자동 생성물, 직접 수정 금지)
├─ 면접대비앱.html             # index.html과 동일 내용(미리보기용)
├─ 앱정보.json                # 버전·제작자·변경이력 (수정하면 앱 상단/자료탭에 반영)
├─ 면접대비_샘플데이터.json     # 공통질문 세트
├─ 면접데이터_경남대_동서대.json # 수록 대학 데이터(대학 추가 시 여기 편집)
├─ 면접사전준비_데이터.json     # ‘사전 준비’ 탭 5단계 내용
├─ 면접일정_2027수시.json       # 달력용 면접 일정(인문/자연)
├─ PROGRESS.md                # 이 문서
└─ 보고서원본/                 # 대학별 선행학습 영향평가 PDF (추출 원본)
```

## 3. 앱을 어떻게 빌드하나 (중요)
`index.html`은 **손으로 고치지 않습니다.** 위 JSON들을 `index.html` 안의
`<script id="seed">` 블록에 합쳐 넣어(주입) 만듭니다. 데이터를 바꾸면 **재주입**해야 반영됩니다.

- 데이터(대학·일정·사전준비·버전)를 바꾸려면 → 해당 JSON 편집 → 재주입 스크립트 실행.
- 화면·기능(HTML/CSS/JS)을 바꾸려면 → `면접대비앱.html`의 해당 부분 편집 → `index.html`로 복사.
- 재주입은 Claude에게 “데이터 반영해서 index 다시 만들어줘”라고 요청하면 됩니다(파이썬으로 seed 블록 교체).

## 4. 대학 추가 방법 (데이터)
`면접데이터_경남대_동서대.json`의 `universities` 배열에 대학 객체 추가.
스키마: `{id, name, color, admissions:[{id,name,type,interviewStyle,scheduleNote,evaluationCriteria:[{name,desc}],dates:[]}], pastQuestions:[{id,year,admission,field,question,modelAnswer,keywords:[]}]}`
- 방침: **학생부·인성 기반 면접만** 포함. 제시문 구술·논술 위주는 제외.
- `modelAnswer`·`keywords`는 학습 지도용(보고서엔 없음). `question`은 원문 충실.

## 5. 대학 확대 현황
`보고서원본/`의 23개 PDF 처리 결과:
- **반영 완료(14, 학생부·인성 기반)**: 경남대, 동서대, 동국대, 광운대, 서울시립대, 성신여대, 국민대, 숙명여대, 덕성여대, 동덕여대, 서경대, 서울여대, 한국외대, 중앙대
- **제외(9, 제시문 구술/논술 위주)**: 서울대, 성균관대, 이화여대, 서강대, 건국대, 서울과기대, 한양대, 경희대, 세종대
- → 제공된 보고서로는 **기본 대학 확대 완료**. 추가 대학은 새 보고서를 받아 같은 방식으로 넣거나, 앱의 ‘자료 → 내 지원 대학 추가’로 개인이 JSON 추가(기기 저장).
- 참고: 학생부·인성 기반 문항은 학생 서류에 맞춘 개별 예시라 `[학생부 기반 예시]`로 표기하고 유형·평가요소 중심으로 담음.

## 6. 배포
`index.html`을 GitHub Pages 또는 Netlify에 올리면 https 주소 생성 → 카톡 공유.
- 스마트폰 마이크·아이폰 Whisper 변환은 https에서만 동작.

## 7. 다른 PC에서 이어서 작업하려면
1. 이 폴더를 **GitHub 저장소**로 push(단일 원본).
2. 다른 PC: Claude 데스크톱 앱 설치 → 저장소를 `git clone` → 그 폴더를 Claude에 연결.
3. 작업 전 `git pull`, 작업 후 `git push`. (동시 편집은 피하고 한 사람씩)
4. 새 Claude 세션에는 대화 기록이 없으니, 이 `PROGRESS.md`를 먼저 읽히면 맥락이 이어집니다.
